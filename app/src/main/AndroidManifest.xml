<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.homeschoolplanner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomeSchoolPlanner">
        <activity android:name=".AddChild"></activity>
        <activity android:name=".CalendarShow"></activity>
        <activity android:name=".AddNewAssignment" />
        <activity android:name=".EditAssignment" />
        <activity android:name=".ChildProfile" />
        <activity android:name=".ParentDashboard" />
        <activity android:name=".SignUpScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>